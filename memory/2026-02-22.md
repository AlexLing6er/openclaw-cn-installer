# 2026-02-22

- Assisted Douhao with low-risk migration planning from US2 to US3 under public bandwidth throttling constraints.
- Verified via SSH read-only checks that US2 (old) and US3 (new) are on the same private subnet and can communicate over private IP (10.0.0.2 <-> 10.0.0.3, SSH reachable internally), enabling intranet migration.
- Audited US2 runtime stack relevant to migration: aaPanel/btpanel, V2bX, x-ui/xray, nginx, MySQL, PostgreSQL, Redis, Docker containers, and dujiao-next services.
- Confirmed aaPanel migration path to US3 is viable but requires post-migration validation for non-panel services (x-ui/V2bX/Docker/custom units).
- User reported aaPanel migration completed on US3 and asked to replicate dujiao-next setup.
- During US3 prep, installed PostgreSQL and Redis dependencies; Redis initially failed due to conflicting /usr/local/lib jemalloc from prior environment, then restored by removing conflicting local jemalloc symlinks/files so Redis links against system lib and starts normally.
- Created/ensured PostgreSQL role and database for dujiao-next on US3; next action pending: private-network data export/import from US2 and systemd unit deployment on US3.
- Security note for future: user shared root credentials in chat; remind credential rotation after migration cutover.
